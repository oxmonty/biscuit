# Code generated by biscuit. DO NOT EDIT.
.PHONY: help build run check vet lint test docs snapshot gacp

# Default target - show help
.DEFAULT_GOAL := help

## Help:
help: ## Show this help message
	@printf "\n\033[1mgalaxy\033[0m\n"
	@printf "%s\n" 'The Scalar Galaxy is an example OpenAPI document to test OpenAPI tools and libraries. It'\''s a fictional universe with fictional planets and fictional data.'
	@printf "\n\033[1mUsage:\033[0m make \033[36m<target>\033[0m\n"
	@awk 'BEGIN {FS = ":.*##"; section=""} \
		/^## [A-Za-z]/ { section=substr($$0, 4); next } \
		/^[a-zA-Z_-]+:.*##/ { \
			if (section != "") { printf "\n\033[1m%s\033[0m\n", section; section="" } \
			printf "  \033[36m%-20s\033[0m %s\n", $$1, $$2 \
		}' $(MAKEFILE_LIST)
	@printf "\n"

## Dev:
build: ## Build all packages
	go build ./...

run: ## Run the CLI locally (Usage: make run ARGS="--help")
	go run ./cmd/galaxy $(ARGS)

## Quality:
test: ## Run all tests
	go test ./...

vet: ## Run go vet
	go vet ./...

lint: ## Run golangci-lint (same linter as CI)
	@command -v golangci-lint >/dev/null || { printf "golangci-lint not installed: brew install golangci-lint\n"; exit 1; }
	golangci-lint run ./...

check: build vet test ## Build, vet, and test (add 'lint' for full CI parity)

## Docs:
docs: ## Generate man pages and shell completions into dist/docsgen
	go run ./scripts/gendocs --dir dist/docsgen

## Release:
snapshot: ## Build a local goreleaser snapshot (no publish, no tag)
	@command -v goreleaser >/dev/null || { printf "goreleaser not installed: brew install goreleaser\n"; exit 1; }
	goreleaser release --snapshot --clean

## Git:
gacp: ## Git add, commit, push (Usage: make gacp M="type(scope): message")
	git add -A && git commit -m "$(M)" && git push
