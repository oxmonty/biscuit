<!-- Code generated by biscuit. DO NOT EDIT. -->
# {{.Binary}}

{{if .Description}}{{.Description}}{{else}}{{.Title}}{{end}}

## Install

{{if .Homebrew -}}
Homebrew 6 requires third-party taps to be trusted before first install:

```sh
brew tap --trust {{.TapOwner}}/tap
brew install {{.TapOwner}}/tap/{{.Binary}}-cli
```

Installing with the fully-qualified form (`brew install {{.TapOwner}}/tap/{{.Binary}}-cli`) skips the trust prompt.

{{end -}}
{{if .InstallScript -}}
```sh
curl -fsSL https://raw.githubusercontent.com/{{.RepoOwner}}/{{.RepoName}}/main/install.sh | sh
```

{{end -}}
{{if or .Homebrew .InstallScript -}}
From source:

{{end -}}
```sh
go install {{.Module}}/cmd/{{.Binary}}@latest
```

## Usage

```sh
{{.Binary}} --help
{{.Binary}} {{.ExampleUse}}
```

## Exit codes

| Code | Meaning |
| ---- | ------- |
| 0 | Success |
| 1 | Internal error |
| 2 | Usage error (bad flags/arguments) |
| 3 | Auth failure (HTTP 401/403) |
| 4 | Other 4xx API error |
| 5 | 5xx API error |
| 6 | Network/transport error |

## Upgrade

```sh
{{.Binary}} upgrade
```

## Shell completions

```sh
{{.Binary}} completion bash|zsh|fish|powershell
```
